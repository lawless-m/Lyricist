[Intro — atmos and sub, no break yet. one line read flat, almost bored]
the dashboard is green

[Build — the break enters. dense, fast, clipped. every one of these is an inference]
the dashboard is green, so it is fine
the ticket is closed, so it is done
it is in the backlog, so it is remembered
it passed the tests, so it works
there is a runbook, so we can recover
the audit passed, so we are secure
the diagram exists, so that is the architecture
the policy is published, so it is followed
the asset is in the register, so we own it
the asset is not in the register
the estimate was agreed, so that is how long it takes
the meeting happened, so a decision was made
the model says four weeks
the model was built from the last one that went well
none of these follow
all of them are load bearing

[Drop — half-time bass under the fast break. single voice, stacked on itself, no crowd, no gang]
SO IT IS FINE
SO IT IS FINE
THAT IS THE SAME THING
SO IT IS FINE

[The breakdown — beat gone. sub gone. one voice, projected and level, not whispered. the only sentence on the record]
I check the dashboard before I check the thing.

[Second drop — bigger, anchors stacked and pitched, no new information, still one voice]
SO IT IS FINE
THAT IS THE SAME THING
SO IT IS FINE
SO IT IS FINE

[Outro — strips back to the atmos it started on. does not resolve]
a system built on a delusion
a delusion built by a system
the second one is much harder to see
